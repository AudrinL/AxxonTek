# Supabase — required for the contact + newsletter forms to persist submissions.
# Create a project at https://supabase.com, run supabase-schema.sql in the SQL editor,
# then copy Project Settings -> API values here (in .env.local, which is gitignored).
NEXT_PUBLIC_SITE_URL=https://axxontek.com
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
